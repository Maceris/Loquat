@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/@TARGETS_EXPORT_NAME@.cmake")

SET(LOQUAT_ROOT_DIR ${PACKAGE_PREFIX_DIR})
SET(LOQUAT_LIBRARIES loquat)
SET(LOQUAT_BUILD_SHARED_LIBS @BUILD_SHARED_LIBS@)
GET_PROPERTY(LOQUAT_INCLUDE_DIRS TARGET loquat PROPERTY INTERFACE_INCLUDE_DIRECTORIES)
SET(LOQUAT_LIBRARY_DIRS "")
